# 新組合（集中學生站）專案規範

本專案屬於英文教學檔案，**必須遵守跨平台相容標準**：學生會用 Windows 電腦、Mac、Android 手機、iPhone/iPad，發音與所有功能都要四平台正常運作。

完整標準（發音手勢解鎖、getVoices 非同步、iOS 兩大地雷、觸控/滑鼠雙支援、responsive 等）見 `~/Desktop/studentgames/CLAUDE.md` 的「跨平台相容標準」與「iOS 單字發音」兩節，開工先讀。

## 確認操作的彈窗規則

- 所有需要使用者確認的操作（例如「查看答案後不計分」）都必須使用網站內自訂彈窗（modal），不可使用瀏覽器原生的 `window.confirm()`／`confirm()`。
- 自訂彈窗必須延續本站既有的視覺風格，並清楚提供「取消」與「確認」按鈕；按鈕文字要描述實際動作，避免使用語意模糊的「好」。
- 彈窗需支援手機、平板與電腦，包含觸控操作、鍵盤焦點，以及窄螢幕版面；開啟時應阻止背景誤觸，關閉後回到原本操作流程。
- 新增或修改確認流程時，必須先搜尋並移除該流程中的原生 `confirm()`，不可讓同一功能同時出現自訂彈窗與瀏覽器原生對話框。

## 出新課必做：lessons.json 的 `all` 陣列 ⚠️

每個學生資料夾 `data/<id>/lessons.json` 有兩個課程清單，**新增課程檔時兩邊都要顧**：

- `units`：本次段考範圍，學生固定連結（`play.html?id=<id>`）只讀這個
- `all`：該資料夾**全部**課程代號。admin 的「🔑 臨時展示（萬用鑰匙）」面板靠它列選項——**新課程代號沒加進 `all`，鑰匙面板就列不出那一課**。學生端不讀 `all`，加了不影響學生

既有課程已全部補齊（2026-07-12）。萬用鑰匙的設計細節見 `PLAN-萬用鑰匙.md`。
